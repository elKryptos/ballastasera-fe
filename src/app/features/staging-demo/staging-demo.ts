import { Component } from '@angular/core';

/**
 * Throwaway component to prove the staging-only flow end to end. Delete once
 * you've built your first real gated feature the same way.
 */
@Component({
  selector: 'app-staging-demo',
  templateUrl: './staging-demo.html',
  styleUrl: './staging-demo.css',
})
export class StagingDemo {}
